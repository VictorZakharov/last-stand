{
  "name": "last-stand",
  "version": "0.1.0",
  "private": true,
  "description": "Last Stand - a 3D wave-survival ARPG arena in the browser (Three.js)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck": "tsc --noEmit",
    "check:pages": "node scripts/check-pages-build.mjs",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "0.185.1"
  },
  "devDependencies": {
    "@types/three": "0.185.4",
    "typescript": "6.0.2",
    "vite": "8.2.2"
  }
}
